var ptype = document.getElementById('ClassID'); var curr = document.getElementById('CurrencyID'); var btntext = document.getElementById('submit'); var curtab = 0; $(document).ready(function () { SetSelection(curtab); }); $("#tabs").tabs(); function SetSelection(x) { ptype.value = curtab = x; if (x == 0) { curr.disabled = true; curr.value = 0; btntext.value = "Create LOCAL"; } else if (x == 1) { curr.disabled = false; btntext.value = "Create EXTERNAL"; } else { curr.disabled = false; btntext.value = "Create FOREIGN"; } } function EnableDropDownBeforeSubmission() { curr.disabled = false; }