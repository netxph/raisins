@model Raisins.Client.Web.Models.PaymentModel

@{
    View.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
	BeneficiaryModel[] Beneficiaries = (BeneficiaryModel[])ViewData["Beneficiaries"];
}

<h2>Delete</h2>
    <h3>Are you sure you want to delete this?</h3>
    <fieldset>
        <legend>Fields</legend>
        
        <!--
		<div class="display-label">ID</div>
        <div class="display-field">@Model.ID</div>
		-->

		<div class="editor-label">
			Team
		</div>
		<div class="editor-field">
			@Html.DropDownListFor(model => model.BeneficiaryID, new SelectList(Beneficiaries, "ID", "Name"))
		</div>

		<div class="display-label">Name</div>
        <div class="display-field">@Model.Name</div>

		<div class="display-label">Amount</div>
        <div class="display-field">@String.Format("{0:0.00}", Model.Amount) @Model.Currency</div>
        
		<!--
        <div class="display-label">Currency</div>
        <div class="display-field">@Model.Currency</div>
        
        <div class="display-label">Email</div>
        <div class="display-field">@Model.Email</div>
        
        <div class="display-label">Location</div>
        <div class="display-field">@Model.Location</div>
		-->

    </fieldset>
    @using (Html.BeginForm()) {
        <p>
            <input type="submit" value="Delete" /> |
            @Html.ActionLink("Back to List", "Index")
        </p>
    }


